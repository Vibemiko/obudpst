# =====================================================================
# Network Access Configuration
# =====================================================================
# This configuration allows access from other devices on your network.
# Replace YOUR_SERVER_IP with your actual server IP address.
# =====================================================================

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend Configuration
# Bind to all interfaces for network accessibility
HOST=0.0.0.0
PORT=3000
NODE_ENV=development

# Frontend Configuration
# Bind to all interfaces
VITE_HOST=0.0.0.0
VITE_PORT=5173

# API URL - Set to your server's IP address
# Example for local network: VITE_API_URL=http://192.168.1.100:3000
VITE_API_URL=http://YOUR_SERVER_IP:3000

# Binary Configuration
UDPST_BINARY_PATH=./udpst

# =====================================================================
# IMPORTANT: Network Access Notes
# =====================================================================
# 1. Find your server IP with: ip addr show (Linux) or ipconfig (Windows)
# 2. Ensure firewall allows connections on ports 3000 and 5173
# 3. Access from other devices using: http://YOUR_SERVER_IP:5173
# =====================================================================
