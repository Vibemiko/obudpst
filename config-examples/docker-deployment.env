# =====================================================================
# Docker Deployment Configuration
# =====================================================================
# This configuration is for running services in Docker containers.
# Uses Docker networking for container-to-container communication.
# =====================================================================

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend Configuration
# Docker containers use 0.0.0.0 to accept connections
HOST=0.0.0.0
PORT=3000
NODE_ENV=production

# Frontend Configuration
VITE_HOST=0.0.0.0
VITE_PORT=5173

# API URL - Leave empty for Docker internal networking
# The nginx.conf handles proxying to 'backend' service
VITE_API_URL=

# Binary Configuration
# In Docker, the binary is mounted at /app/udpst
UDPST_BINARY_PATH=/app/udpst

# Docker-Specific Settings
DOCKER_BACKEND_HOST=0.0.0.0
DOCKER_FRONTEND_API_URL=http://backend:3000

# =====================================================================
# Docker Access Information
# =====================================================================
# After starting with docker-compose:
#   - Frontend: http://localhost (port 80)
#   - Backend API: http://localhost:3000
#   - Health Check: http://localhost:3000/health
# =====================================================================
